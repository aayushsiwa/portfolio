import AdminPage from "@/containers/Admin/Admin";

/**
 * Renders the admin interface page.
 *
 * @returns The rendered AdminPage element.
 */
export default function HomePage() {
  return <AdminPage />;
}
