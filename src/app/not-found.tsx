import NotFound from "@/containers/NotFound/404";

/**
 * Renders the application 404 Not Found page.
 *
 * @returns The JSX element for the NotFound page
 */
export default function NotFoundPage() {
  return <NotFound />;
}
