import NotFound from "@/containers/NotFound/404";

/**
 * Render the application's 404 Not Found page.
 *
 * @returns A React element that displays the 404 Not Found page
 */
export default function NotFoundPage() {
  return <NotFound />;
}
