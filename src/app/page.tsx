import Home from "@/containers/Home";

/**
 * Renders the application's Home container component.
 *
 * @returns A JSX element that renders the `Home` component.
 */
export default function HomePage() {
  return <Home />;
}
