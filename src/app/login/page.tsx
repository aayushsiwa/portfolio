import Login from "@/containers/Admin/Login/Login";

/**
 * Renders the admin Login container.
 *
 * @returns A JSX element that renders the `Login` component.
 */
export default function LoginPage() {
  return <Login />;
}
